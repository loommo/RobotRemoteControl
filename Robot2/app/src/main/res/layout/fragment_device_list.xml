<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:background="#FFE4E1"
    tools:context=".ui.fragment.DeviceListFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/list_device"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

    <Button
        android:id="@+id/button_find_device"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/list_device"
        android:background="#ffffff"
        android:text="查找设备" />

    <ProgressBar
        android:id="@+id/progress_find_device"
        style="@android:style/Widget.Holo.ProgressBar.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/button_find_device"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/button_find_device"
        android:layout_marginRight="60dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/button_cancel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_find_device"
        android:layout_marginTop="2dp"
        android:background="#ffffff"
        android:text="取消" />

</RelativeLayout>
